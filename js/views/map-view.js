/**
 * Created by SpinyNorman on 25/11/2016.
 */
// allows rendering of the list
// update lists based on search terms
// implements event listener (input, select/toggle, infoWindow


class MapView {
    'use strict';

    constructor () {
        //Define list (not necessary => knockout)
        //Define search box for event handlers (not necessary => knockout)
        //Define functions what happens when knockout kicks in
    };
};

export default MapView();